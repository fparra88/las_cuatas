# Single-service: frontend servido por backend, API relativa (/api).
# VITE_API_URL YA NO se usa. Dejar vacio/relativo.
# Solo descomenta si algun dia separas backend a otro host:
# VITE_API_URL=http://localhost:8000
#prueba
